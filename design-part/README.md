# Laravel Workshop

## Local setup

This is a minimal Vite app.

```sh
Use Node.js v22 or later, as this project relies on features required by Vite and modern tooling.
```
To run it locally, first install the dependencies:

```sh
npm install
```

Then, start the dev server:

```sh
npm run dev
```